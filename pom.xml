<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.4</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>

    <groupId>edu.si.ossearch</groupId>
    <artifactId>OSSearchv2</artifactId>
    <version>${revision}</version>
    <packaging>pom</packaging>

    <name>OSSearchv2</name>
    <description>Demo project for Spring Boot</description>

    <modules>
        <module>nutch-plugins</module>
        <module>ossearch-app-frontend-vue3</module>
        <module>ossearch-crawler</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <revision>0.1.9-SNAPSHOT</revision>
        <nutch.version>1.18</nutch.version>
        <hadoop.version>3.4.1</hadoop.version>
        <!--
            Tests remain skipped by default (unchanged behavior), but this is now a property
            rather than a hardcoded literal in the surefire config below, so a test run can
            actually be requested. This property is deliberately named "ossearch.skipTests"
            rather than "skipTests": "skipTests" is surefire's own well-known system property,
            and since this configuration lives in the ROOT pluginManagement it is inherited by
            every module, including the legacy nutch-plugins/* modules whose pre-existing tests
            are broken. If this were named "skipTests", passing -DskipTests=false to run the
            ossearch-crawler tests would also un-skip (and break) those legacy modules. The
            project-specific name avoids colliding with surefire's own property, so only an
            explicit -Dossearch.skipTests=false opts a module in.
        -->
        <ossearch.skipTests>true</ossearch.skipTests>
    </properties>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-dependency-plugin</artifactId>
                    <version>3.2.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <configuration>
                        <encoding>UTF-8</encoding>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                </plugin>
                <plugin>
                    <groupId>com.googlecode.maven-download-plugin</groupId>
                    <artifactId>download-maven-plugin</artifactId>
                    <version>1.6.6</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>2.22.2</version>
                    <configuration>
                        <skipTests>${ossearch.skipTests}</skipTests>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>
